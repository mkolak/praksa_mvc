<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Cars
    </title>
  </head>
  <body>
    <form method="post">
      {% for field in fields %}
        <label for="{{ field.inputName }}">{{ field.name }}</label><br />
        <input type="{{ field.inputType }}" for="{{ field.inputName }}" /><br />
      {% endfor %}
      <input type="submit"></input>
    </form>
  </body>
</html>
